{
  "name": "campusdesk-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend + MongoDB Atlas database for the CampusDesk complaint tracker",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "connect-mongo": "^5.1.0",
    "express": "^4.19.2",
    "express-session": "^1.18.1",
    "mongodb": "^6.18.0",
    "nodemailer": "^9.0.5"
  }
}
